This app performs CRUD operations of 4 parameters.

To start the application
1. Do an npm install from the project directory
2. Modify the cloud mongodb URL and firebase storage bucket url 
3. Then do npm start